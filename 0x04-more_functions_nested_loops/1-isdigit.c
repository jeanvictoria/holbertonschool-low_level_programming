#include "holberton.h"
/**
 * isdigit - fuction for evaluate if have digit.
 * @c: variable to evaluate
 *
 * Return: Always 0.
 */
int _isdigit(int c)
{
	if (c >= 40 && c <= 57)
	{
	return (1);
	}
return (0);
}
